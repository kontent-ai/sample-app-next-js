# Supply-chain defense: refuse package versions published less than 3 days ago,
# giving the registry and security scanners time to catch malicious releases.
# Requires npm >= 11.10.0; older npm ignores this key.
min-release-age=3
